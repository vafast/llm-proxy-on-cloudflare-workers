{
  "name": "llm-proxy-on-cloudflare-workers",
  "author": "blue.pen5805",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "npm run secrets:dev-vars -- --env develop && wrangler dev --env develop",
    "cf-typegen": "npm run secrets:dev-vars -- --env example && wrangler types --env example",
    "cf:login": "wrangler login",
    "cf:logout": "wrangler logout",
    "test": "vitest run",
    "test-watch": "vitest watch",
    "prettier": "prettier --write .",
    "prettier-ci": "prettier --check .",
    "lint": "eslint 'src/**/*.ts'",
    "lint:fix": "eslint --fix 'src/**/*.ts'",
    "secrets:deploy": "ts-node scripts/deploy-secrets.ts",
    "secrets:dev-vars": "ts-node scripts/generate-dev-vars.ts",
    "secrets:create": "ts-node scripts/create-config.ts",
    "download-llm-resources": "ts-node scripts/download-llm-resources.ts"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.49",
    "@trivago/prettier-plugin-sort-imports": "^5.2.2",
    "@types/node": "^24.0.10",
    "@typescript-eslint/eslint-plugin": "^8.35.1",
    "@typescript-eslint/parser": "^8.35.1",
    "eslint": "^9.30.1",
    "eslint-config-prettier": "^10.1.5",
    "eslint-plugin-prettier": "^5.5.1",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.35.1",
    "vite": "^7.0.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.23.0"
  }
}
