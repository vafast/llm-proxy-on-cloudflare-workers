# Verification Rules

Before completing any task that involves code changes, you MUST:

1. Execute the verification workflow:
   - Run `@/verify` to ensure prettier, lint, and tests all pass.

2. Record the results (success) in the `walkthrough.md` as proof of verification.
